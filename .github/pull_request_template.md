## Summary

<!-- 简要说明本 PR 做了什么、为什么 -->

## Test plan

- [ ] `cd deskbot-server && ruff check src && python -m compileall -q src`
- [ ] `./start.sh` 或 `SKIP_SETUP=1 ./start.sh` 本地冒烟
- [ ] （若改协议）更新了 `docs/` 或 README
